import{NextResponse}from'next/server';import{getOpenAI,getModel}from'@/lib/ai/openai';import{ASSISTANT_PROMPT}from'@/lib/ai/prompts';
export async function POST(req:Request){try{const{question,context}=await req.json();if(!question)return NextResponse.json({error:'question_required'},{status:400});const r=await getOpenAI().responses.create({model:getModel(),instructions:ASSISTANT_PROMPT,input:`AVAILABLE CONTEXT:\n${JSON.stringify(context||{})}\n\nPARTICIPANT:\n${question}`});return NextResponse.json({answer:r.output_text})}catch(error){console.error('assistant_error',error);return NextResponse.json({error:'assistant_failed'},{status:500})}}
