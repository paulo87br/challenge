# Supabase — browser needs the NEXT_PUBLIC_ prefix for SSO to work
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Server only. Bypasses RLS; used to write Observer evidence. Never expose.
SUPABASE_SERVICE_ROLE_KEY=

OPENAI_API_KEY=
OPENAI_MODEL=
